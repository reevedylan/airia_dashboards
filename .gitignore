# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# editor / OS
.DS_Store
.vscode/
.idea/

# local env
.env
.env.local

# ingest cache, if a snapshot script is ever reintroduced
.cache/

# no tenant data is written to disk any more, but keep these so a stray
# snapshot cannot be committed by accident
public/data/*.json
